// 模拟银行窗口接待业务
// 假设某银行有四个窗口对外接待客户，
// 从早晨银行开门起，不断有客户进入银行，
// 由于每个窗口在某个时刻只能接待一个客户，
// 因此在客户人数众多时，须在每个窗口前顺序排队，
// 对于刚进入银行的客户。
// 如果某个窗口的业务员正在空闲，可上前办理业务，
// 反之，若是个窗口均有客户所占,便会排在人数最少的队伍后面.
// 现需要编制一个程序，以模拟银行的这种业务活动，
// 并计算一天中客户在银行逗留的平均时间

// 算法示意如下：
// 为了计算这个平均时间，
// 我们自然需要掌握每个客户到达银行和离开银行这两个时刻，后者减去前者，既为每个客户在银行的逗留时间，
// 所有客户逗留时间的总和除以进入银行的客户数。便是所求的平均时间，
// 称客户到达银行和离开银行这两个时刻发生的事情，
// 整个模拟程序将按事件发生的先后顺序进行处理，这样一种模拟程序成为事件驱动模拟

// todo
void bank_simulation()
{
    // 初始化

    // 

    // 结束
}